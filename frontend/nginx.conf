server {
  listen 80;
  server_name sri-frontend;
  root /usr/share/nginx/html/;
  index index.html index.html;

  location /api {
    proxy_pass http://sri-backend:3000;
  }

  location / {
    try_files $uri $uri/ /index.html =404;
  }
}